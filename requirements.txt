streamlit>=1.21.0
altair<5
pytest

# The AI Glitch Investigator uses Claude when a key is present. It is optional:
# without it, the system falls back to a deterministic offline mock and all
# tests still pass. Install to enable live Claude reasoning.
anthropic
